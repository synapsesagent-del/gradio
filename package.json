{
  "name": "devops-sample-app",
  "version": "1.0.0",
  "description": "Sample application for DevOps pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}